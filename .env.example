# Multi-Provider OIDC Configuration
# Configure any or all of the providers below to enable them
# You can have multiple providers active simultaneously

# Google
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5001/auth/google/callback

# Microsoft Azure AD
AZURE_CLIENT_ID=your-azure-client-id
AZURE_CLIENT_SECRET=your-azure-client-secret
AZURE_TENANT_ID=common
AZURE_MULTITENANT=true
AZURE_REDIRECT_URI=http://localhost:5001/auth/azure/callback

# JWT Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
JWT_EXPIRATION=24h

# SMTP Email Configuration (for OTP verification emails) using Gmail --> Generate App Password at https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-app-password-here
SMTP_FROM_NAME=KampungConnect

# Database Configuration - local PostgreSQL Docker Container
DB_HOST=db
DB_PORT=5432
DB_NAME=kampungconnect
DB_USER=admin
DB_PASSWORD=password

# AWS RDS Configuration
AWS_DB_HOST=<smth>.ap-southeast-2.rds.amazonaws.com
AWS_DB_PORT=5432
AWS_DB_NAME=kampungconnect
AWS_DB_USER=your_actual_aws_username
AWS_DB_PASSWORD=your_actual_aws_password

# Service URLs
AUTH_SERVICE_URL=http://localhost:5001
FRONTEND_URL=http://localhost:8080
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672
PORT=5003